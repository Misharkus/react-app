{
  "name": "react-app-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "server": "json-server --watch db.json -p 5174"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
